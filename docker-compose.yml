services:
  rss-reader:
    build: .
    restart: unless-stopped
    ports:
      - "4321:4321"
    volumes:
      - ./data:/app/data
    environment:
      HOST: "0.0.0.0"
      PORT: "4321"
    security_opt:
      - apparmor=unconfined
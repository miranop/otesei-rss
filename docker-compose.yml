services:
  rss-reader:
    build: .
    restart: unless-stopped
    ports:
      - "4321:4321"
    volumes:
      - ./data:/app/data
    environment:
      HOST: "0.0.0.0"
      PORT: "4321"
    sysctls:
      - net.ipv4.ip_unprivileged_port_start=0